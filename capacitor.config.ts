import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.albertodeveloper.mathtetris',
  appName: 'MathTetris',
  webDir: 'out'
};

export default config;
